{
  "name": "redcode-ts",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "peggy": "^4.2.0"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "build": "peggy -o gen/redcode.js --format es --dts redcode.peggy"
  }
}
